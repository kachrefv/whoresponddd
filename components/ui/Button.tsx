import React from 'react';interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {  variant?: 'primary' | 'secondary' | 'outline';  size?: 'sm' | 'md' | 'lg';  children: React.ReactNode;}const Button: React.FC<ButtonProps> = ({  variant = 'primary',  size = 'md',  children,  className = '',  ...props}) => {  const baseStyles =    'inline-flex items-center justify-center font-semibold rounded-lg transition-all transform';  const variantStyles = {    primary: 'bg-ios-blue text-white hover:opacity-90 shadow-lg hover:scale-105',    secondary:      'bg-ios-panel-contrast dark:bg-ios-panel-contrast-dark text-ios-blue hover:bg-gray-200 dark:hover:bg-ios-panel-dark border border-ios-border dark:border-ios-border-dark hover:scale-105',    outline:      'bg-transparent text-ios-blue border border-ios-blue hover:bg-ios-blue hover:text-white',  };  const sizeStyles = {    sm: 'px-4 py-2 text-sm',    md: 'px-6 py-3 text-base',    lg: 'px-8 py-3.5 text-lg',  };  return (    <button      className={`${baseStyles} ${variantStyles[variant]} ${sizeStyles[size]} ${className}`}      {...props}    >      {children}    </button>  );};export default Button;